import React from "react";
import styled from "styled-components";
import palette from "../../lib/styles/palette";

const StyledTitle = styled.div`
  font-weight: bold;
  padding: 1rem 1.3rem;
  font-size: 1.3rem;
  margin-top: 1rem;
`;
const AddCard = () => {
  return <StyledTitle>더보기</StyledTitle>;
};

export default AddCard;
